// config.js — point this at your running backend (see server/README.md).
// Leave it as-is and the app works standalone using only local storage,
// same as before.
window.MAALWALA_API_BASE = ''; // e.g. 'http://localhost:4000'
